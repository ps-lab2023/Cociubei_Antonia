import React from 'react';

const Home = () => {
    return (
        <div style={{ width: '100vh', height: '100vh', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
            <h1>Welcome️ to BookDespository! 🚀</h1>
        </div>
    );
};

export default Home;
